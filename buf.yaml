version: v2
modules:
  - path: api/proto
lint:
  use:
    - STANDARD
  ignore:
    # Vendored googleapis protos; not ours to lint.
    - api/proto/google
breaking:
  use:
    - FILE
  ignore:
    - api/proto/google
