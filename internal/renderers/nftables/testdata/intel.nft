#!/usr/sbin/nft -f
# Generated by OpenNGFW controld. Do not edit; overwritten on every commit.

table inet openngfw
delete table inet openngfw

table inet openngfw {
	set intel4 {
		type ipv4_addr
		flags interval
	}

	set intel6 {
		type ipv6_addr
		flags interval
	}

	chain input {
		type filter hook input priority filter; policy accept;
		iifname "lo" accept
		ct state established,related accept
		ct state invalid drop
	}

	chain forward {
		type filter hook forward priority filter; policy drop;
		ct state established,related accept
		ct state invalid drop
		ip saddr @intel4 log prefix "ngfw:intel-block: " counter drop comment "intel-block-src"
		ip daddr @intel4 log prefix "ngfw:intel-block: " counter drop comment "intel-block-dst"
		ip6 saddr @intel6 log prefix "ngfw:intel-block: " counter drop comment "intel-block-src6"
		ip6 daddr @intel6 log prefix "ngfw:intel-block: " counter drop comment "intel-block-dst6"
		iifname "eth1" oifname "eth0" counter accept comment "rule:lan-to-wan id=rule-lan-to-wan-a7feb9a32169"
		counter comment "default-drop"
	}
}
